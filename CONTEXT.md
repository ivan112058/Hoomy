# Hoomy 领域术语表

本文件是**术语表**：只收录领域词汇与视觉/交互约定，不含实现细节、验收标准或功能范围。功能范围见 spec，技术决策见 `docs/adr/`。

## 术语

**Hoomy**：
局域网 NAS 音乐播放器。客户端 Flutter，服务端 Navidrome，经 Subsonic 兼容 API 通信。

**服务器（Server）**：
一台 Navidrome 实例，由 URL + 用户名 + 密码标识。

**歌曲（Song）**：
一首可播放的音轨，格式仅 flac、mp3、m4a。

**专辑（Album）**：
歌曲的分组，含封面与服务端解析的内嵌信息。

**歌手（Artist）**：
按演唱/演奏者聚合的歌曲分组。

**内嵌数据（Embedded Data）**：
存储在音频文件内、由**服务端**解析后经 API 返回的数据（metadata、封面、歌词）。
_Avoid_：本地解析、客户端解析 —— 客户端不读取音频文件本身，此处的「内嵌」描述数据的来源，不描述客户端的取用方式。

**内嵌封面（Embedded Cover）**：
歌曲所属专辑的封面图，服务端解析并提供，客户端经 `getCoverArt` 取得。

**内嵌歌词（Embedded Lyrics）**：
服务端解析并随歌词接口返回的歌词。服务端除文件内嵌标签外，也解析同名侧车文件（`.lrc`/`.txt`/`.srt`/`.ttml` 等），客户端无法也无需区分来源。

**歌词档位（Lyric Tier）**：
客户端按内容可用性呈现歌词的三种形态：纯文本静态、逐行（LRC）滚动、逐字（karaoke）高亮。档位由服务端返回的歌词内容决定，不由服务端版本决定。
_Avoid_：三档全部支持 —— 逐字要求曲目本身携带逐字时间轴，多数曲目只有行级时间轴。

**收藏（Star）**：
对歌曲/专辑/歌手的喜爱标记，保存在服务端，跨设备同步。

**我喜欢的歌曲（Loved Songs）**：
用户收藏（star）过的歌曲集合。

**播放队列（Queue）**：
当前待播放的歌曲有序列表，可拖拽排序。
_Avoid_：播放列表 —— 那是 Playlist。

**播放列表（Playlist）**：
用户整理并保存在服务端的歌曲合集。与一次性的播放队列相区别。

**风格（Genre）**：
按流派聚合的歌曲分组。

**服务器证书（Server Certificate）**：
仅接受标准证书校验，不支持信任自签证书。

## 视觉与交互约定

对齐 SmartisanMusic-Revived 的设计语言。数值为约定值，实现方式不限。

**方块化**：
界面基本无圆角，列表、标题栏、按钮均为直角。
_Avoid_：圆角卡片。

**按压反馈**：
列表行按下时整行变蓝底、文字与图标变白。
_Avoid_：仅涟漪、灰色高亮。

**列表行**：
歌曲行高 60dp，**不含封面缩略图**；标题 16sp、副标题 13sp、时长 13sp 加粗；副标题为「歌手 - 专辑」。

**分隔线**：
列表分隔线 0.67dp，浅色 `#ebebeb`、深色 `#3a3d42`，**整宽无缩进**。

**专辑网格**：
固定 3 列，不随屏宽改变列数，封面 1:1。

**标题栏**：
高度 50dp，标题 20sp。

**浅色优先**：
浅色为默认；深色跟随系统，也可手动切换。

**播放页**：
静止封面展示，无唱盘旋转、唱针、搓碟与音效交互。
_Avoid_：黑胶、唱盘、唱针、搓碟。

**迷你播放条**：
位于底部 Tab 之上的常驻控制条，显示当前曲目封面、歌名、歌手与播放控制，不显示进度。
_Avoid_：底部播放条、Now Playing Bar。
