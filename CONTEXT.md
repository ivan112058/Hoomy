# Hoomy 领域术语表

本文件仅收录领域词汇，不包含实现细节。

## 术语

- **Hoomy**：局域网 NAS 音乐播放器 App，客户端为 Flutter，服务端为 Navidrome。
- **服务器（Server）**：一台 Navidrome 实例，由 URL + 用户名 + 密码标识。用户登录后获得访问凭据。
- **歌曲（Song）**：一首可播放的音轨，格式仅限 flac、mp3、m4a。其 metadata（标题、歌手、专辑、音轨号等）由服务端从文件内嵌标签解析，客户端经 API 读取。
- **专辑（Album）**：歌曲的分组，含封面与服务端解析的内嵌信息。
- **歌手（Artist）**：按演唱/演奏者聚合的歌曲分组。
- **内嵌封面（Embedded Cover）**：存储在音频文件内的封面图，由服务端解析并提供，客户端不解析音频文件本身。
- **内嵌歌词（Embedded Lyrics）**：存储在音频文件内的歌词，由服务端解析并随歌词接口返回；可能为纯文本、逐行（LRC）或逐字（增强 LRC）格式。
- **收藏（Star）**：对歌曲/专辑/歌手的喜爱标记，保存在服务端，跨设备同步。
- **播放队列（Queue）**：当前待播放的歌曲有序列表，可拖拽排序。
- **播放列表（Playlist）**：用户整理的歌曲合集（与一次性播放队列相区别）。
- **风格（Genre）**：按流派聚合的歌曲分组。
- **我喜欢的歌曲（Loved Songs）**：用户收藏（star）过的歌曲集合。

- **播放页**：静止封面展示，无唱盘旋转、唱针、搓碟与音效交互。
- **歌词三档**：纯文本静态、逐行 LRC 滚动、逐字 karaoke 高亮，全部支持。
- **流传输**：仅请求原始流，不转码；歌曲不做磁盘缓存（纯流式）。
- **封面缓存**：封面磁盘缓存，需提供清除缓存入口。
- **账户**：MVP 单服务器单用户；多账户为后续扩展。
- **后台播放与锁屏控制**：MVP 必含（iOS audio_session、桌面系统媒体控制）。
- **迭代顺序**：iOS 先立稳视觉语言、数据层与播放；Android TV 第二迭代做布局适配。
- **底部 Tab**：播放列表、艺术家、专辑、歌曲、更多；更多页含风格、我喜欢的歌曲，左上角为设置入口。后续迭代支持长按拖动重排 Tab（“更多”固定不可移）。
- **HTTPS**：仅标准证书校验，不支持自签信任。
- **砍掉的功能**：睡眠定时、均衡器、评分、搓碟/唱针交互、滑动多选/删除。
- **列表交互**：保留蓝色按压反馈、字母快捷栏；专辑页 3 列网格。
- **歌单**：MVP 含服务端歌单只读（列表、查看曲目、播放）；创建/编辑/删除后续迭代。
- **更多页**：风格、我喜欢的歌曲在 MVP 即实现（含歌曲行/播放页的 star 收藏动作）。

## 已定决策（决策记录见 docs/adr/）

- API 协议：Subsonic 兼容 API。
- 客户端不自行解析音频文件标签；一切内嵌数据经服务端 API 获取。
- 播放内核：media_kit（四平台统一）。
- 状态管理：Riverpod；按 feature 分层 + repository。
- MVP：登录、歌曲/专辑/歌手浏览、播放（含队列）、歌词、封面。搜索与收藏第二迭代。
- Android TV 使用独立布局骨架（D-pad 导向），复用视觉 token。
- 主题：浅色优先、深色跟随系统；UI 文案仅中文。
- 平台优先级：iOS > Android TV > macOS > Windows。
